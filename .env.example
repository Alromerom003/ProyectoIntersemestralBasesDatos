# Copia este archivo como .env y reemplaza los valores
# cp .env.example .env   (Linux/Mac)
# copy .env.example .env (Windows)

DATABASE_URL=postgresql+psycopg://postgres:TU_PASSWORD@localhost:5432/pagila
